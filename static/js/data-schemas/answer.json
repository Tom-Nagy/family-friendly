{
    "bsonType": "object",
    "required": ["_id", "question", "answer_message"],
    "properties": {
        "_id": {"bsonType": "objectId"},
        "question_category": {
            "bsonType": "string",
            "description": "must be a string, match the expression pattern if the field exists"
        },
        "question": {
            "bsonType": "string",
            "description": "must be a string, match the expression pattern and is required"
        },
        "answer_message": {
            "bsonType": "string",
            "description": "must be a string, match the expression pattern and is required"
        },
        "answer_picture": {
            "bsonType": "string",
            "description": "must be a string, match the expression pattern if the field exists"
        },
        "answer_created_by": {
            "bsonType": "objectId"
        },
        "answer_likes": {
            "bsonType": "array",
            "description": "must be an array if field exists"
        }
    }
}