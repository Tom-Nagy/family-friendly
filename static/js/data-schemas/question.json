{
    "bsonType": "object",
    "required": ["_id", "question_category", "question", "question_description"],
    "properties": {
        "_id": {"bsonType": "objectId"},
        "question_category": {
            "bsonType": "string",
            "description": "must be a string, match the expression pattern if the field exists"
        },
        "question": {
            "bsonType": "string",
            "description": "must be a string, match the expression pattern and is required"
        },
        "question_description": {
            "bsonType": "string",
            "description": "must be a string, match the expression pattern and is required"
        },
        "question_picture": {
            "bsonType": "string",
            "description": "must be a string, match the expression pattern if the field exists"
        },
        "question_created_by": {
            "bsonType": "objectId"
        },
        "question_likes": {
            "bsonType": "array",
            "description": "must be an array if field exists"
        }
    }
}